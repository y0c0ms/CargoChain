# Dependencies
node_modules/

# Local env / secrets
.env
.env.local
.env.*.local

# Hardhat artifacts
artifacts/
cache/
typechain-types/
coverage/
coverage.json

# Next.js
.next/
out/

# TypeScript incremental build cache
*.tsbuildinfo

# Circom build output
circuits/build/

# Oracle simulator output (regenerated at runtime, can be MBs of JSON)
prototype/app/public/oracle-batches/*.json
!prototype/app/public/oracle-batches/.gitkeep

# Package manager lock files (project uses npm)
bun.lock

# IDE / OS / agent tooling
.DS_Store
Thumbs.db
*.log
dist/
.claude/
**/.claude/
.mcp.json
docs/superpowers/

# Slide drafts kept locally only
SLIDES_INTERMEDIATE.md
SLIDES_FINAL.md
logs/
